/**
 * OUTDOOR — Section 2 Animation Slot (/plays)
 * ─────────────────────────────────────────────
 * Drop your custom animation file here and call init() below.
 *
 * The target element is:   <div class="plays-placeholder" id="plays-slot">
 * It lives in Section 2 (Value Proposition) of index.html.
 *
 * To integrate:
 *   1. Add your animation script to this folder.
 *   2. In index.html, replace:
 *        <script src="plays/placeholder.js"></script>
 *      with your actual script tag.
 *   3. Mount your animation onto the #plays-slot element.
 *
 * Example mount:
 *   const slot = document.getElementById('plays-slot');
 *   myAnimation.init(slot);
 */
